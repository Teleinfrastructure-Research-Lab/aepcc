architecture:
  name: folding-net
  module_params:
    pe_type: sphere
    num_features: 512
    quantize: !root_include "../quantize.yaml"
  load_ckpt: !link checkpoints->foldingnet512

dataloader-test:
  name: default-dl
  dl_params:
    split: "test"

dataloader-train:
  name: default-dl
  dl_params:
    split: "train" 